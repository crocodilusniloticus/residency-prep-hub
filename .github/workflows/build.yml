name: Build Status

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    # 1. Check out the code to the runner
    - uses: actions/checkout@v3
    
    # 2. (Optional) Setup language - Uncomment if needed
    # - name: Set up Python
    #   uses: actions/setup-python@v4
    #   with:
    #     python-version: '3.10'

    # 3. Run a simple script (Replace with your actual test command)
    - name: Run Sanity Check
      run: echo "Build successful. Code is accessible."